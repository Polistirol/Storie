# Dipendenze inference incluse nell'env unificato del repo.
# Dalla root Storie/:
#   micromamba create -f environment.yml
#   pip install -r requirements.txt
#
# Solo inference (se l'env esiste già):
-r ../requirements.txt
